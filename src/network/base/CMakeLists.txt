# cmake_minimum_required(VERSION 2.6)
# project(network_base)

# aux_source_directory(. BASE_LIB_SRCS)

# # CMAKE_SOURCE_DIR 指的是顶层cmake所在的目录
# set(LIBRARY_OUTPUT_PATH ${CMAKE_SOURCE_DIR}/lib/network_base)

# add_library(network_base ${BASE_LIB_SRCS})
