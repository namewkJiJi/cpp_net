# cmake_minimum_required(VERSION 2.6)
# project(net)

# aux_source_directory(. NET_LIB_SRCS)

# include_directories(${CMAKE_SOURCE_DIR}/src/network/base)

# CMAKE_SOURCE_DIR 指的是顶层cmake所在的目录
# set(LIBRARY_OUTPUT_PATH ${CMAKE_SOURCE_DIR}/lib/network)

# add_library(net ${NET_LIB_SRCS})
